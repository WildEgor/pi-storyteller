version: '3.9'

services:
  builder:
    build: 
      context: .
      dockerfile: Dockerfile
      target: app
    volumes:
      - ./out:/app/out
      # - ./weights:/app/weights
      - ./scripts:/app/scripts
      # - ./bin:/app/OnnxStream/src/build
    stdin_open: true
    tty: true
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '0.50'
    #       memory: 512M
    #     reservations:
    #       cpus: '0.25'
    #       memory: 256M